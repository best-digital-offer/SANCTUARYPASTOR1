# Supabase (publishable/anon key — safe to expose client-side)
NEXT_PUBLIC_SUPABASE_URL=https://kbvftcalklxnrwmnsxar.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_yH7DSqg00CAVqF0NW3EZFA_LTwvPrAU

# Get this from Supabase → Project Settings → API. NEVER expose this one to the browser
# and never commit it — server-only, used for admin operations (e.g. free-prayer enforcement).
SUPABASE_SERVICE_ROLE_KEY=

# Puter.js needs no API key — it's loaded client-side via <script src="https://js.puter.com/v2/">

# --- Phase 2+ (leave blank until you're ready to wire these in) ---
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

AVATAR_API_KEY=
EMAIL_API_KEY=
